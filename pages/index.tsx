import type { NextPage } from "next";
import Head from "next/head";

const Home: NextPage = () => {
  return (
    <div>
      <Head>
        <title>Noted</title>
      </Head>
    </div>
  );
};

export default Home;
